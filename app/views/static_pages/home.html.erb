<% provide(:title, "Home") %>

<% if logged_in? %>
  <%= render "shared/dashboard" %>
<% else %>
  <div class="center jumbotron">
    <h1>Fitness App</h1>

    <h2>
      This is the home page for the Fitness App
    </h2>

    <%= link_to "Sign up now!", signup_path, class: "btn btn-lg btn-primary" %>
  </div>
<% end %>
